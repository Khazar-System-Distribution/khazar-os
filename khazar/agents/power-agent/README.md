# AI Power Agent

System management agent for the Khazar AI platform. Handles shutdown, reboot, suspend, hibernate, lock session, and logout via `systemctl` and `loginctl`.

## Build

```sh
make
```

## Socket

`/run/ai-power-agent.sock`

## License

GPL-3.0-only
