# AI Package Agent

Package management agent for the Khazar AI platform. Handles `install_package`, `remove_package`, `search_package`, and `system_update` operations using either `apt` or `pacman`.

## Build

```sh
make
```

## Socket

`/run/ai-package-agent.sock`

## License

GPL-3.0-only
